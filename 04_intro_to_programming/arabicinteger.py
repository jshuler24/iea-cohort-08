#! /usr/bin/python3

#create a dictionary to store arabic values and roman numerals
# Load the dict with Roman numerals M (1000), D (500), C (100), L (50), X (10), V (5), I (1) 

arabic_dict = {1000 : 'M', 500 : 'D', 100 : 'C', 50 : 'L' , 10 : 'X', 5 : 'V', 1 : 'I'}

# ask for user input for a arabic integer
#store user input into a variable
arabic_int = (input('Please enter an arabic integer:'))
# loop over integer
	# starting at position 0
	# replace arabic integers with roman numerals
	# after loop print out the roman numeral equivalent
final_answer = ""
for word in (arabic_int.split()):
    final_answer.append(arabic.int[word])
    






