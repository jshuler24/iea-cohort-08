/bin/bash

# if [ ]; then
# printf " argument expected" 

# test -f /path/to/file && echo "file exists." || echo "file does not exist"
if test -f /home/jshuler/iea-cohort-08/02_linux_bash_scripting/linux-files/foo.txt; then
    echo$ " file exists" 
else
    echo$ " file does not exist" 
fi
